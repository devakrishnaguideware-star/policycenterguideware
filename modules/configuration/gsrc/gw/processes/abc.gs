package gw.processes

class abc extends BatchProcessBase {

  construct() {
    super(BatchProcessType.TC_ABC);
  }

  protected override function doWork() {
    var pp : PolicyPeriod
    var job = pp.Policy
    var exp = Date

    if (pp.PolicyEndDate == ) {

    }
  }
}